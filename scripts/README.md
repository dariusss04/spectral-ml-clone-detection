# Scripts

- `training_scripts/` — Training entrypoints for all architectures.
- `clone_search/` — Clone search evaluation scripts.
- `meta_generation/` — Metadata helpers (split configs, summaries).
